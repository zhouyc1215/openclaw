# 飞书定时任务问题完整解决方案

## 项目概述

本文档记录了飞书定时任务从错误 798、520 到完全正常运行的完整修复过程。

## 问题时间线

### 2026-02-27 上午：问题发现

- 飞书持续收到错误消息："unknown error, 798 (1000)"
- 每小时整点触发，影响用户体验

### 2026-02-27 中午：问题解决

- 完成 5 个关键问题的修复
- 所有 4 个定时任务测试通过
- 系统恢复正常运行

---

## 修复的 5 个核心问题

### 问题 1: 错误 798 - 缺少发送目标 ✅

**症状**:

```
unknown error, 798 (1000)
```

**根本原因**:

- 定时任务配置了 `delivery.channel: "last"` 但缺少明确的 `delivery.to` 目标
- 错误 798 是飞书 API 的错误代码，表示发送目标无效或缺失
- (1000) 是用户的 UID，来自临时目录名 `/tmp/openclaw-1000/`

**修复方案**:

```json
{
  "delivery": {
    "mode": "announce",
    "channel": "feishu",
    "to": "ou_b3afb7d2133e4d689be523fc48f3d2b3"
  }
}
```

**影响文件**:

- `~/.openclaw/cron/jobs.json`

---

### 问题 2: 错误 520 - Gateway 超时 ✅

**症状**:

```
unknown error, 520 (1000)
```

**根本原因**:

- 所有 4 个定时任务运行在 `isolated` 模式
- 任务执行时间为 58-156 秒
- Gateway 默认超时时间为 60 秒
- 任务超时导致 520 错误

**修复方案**:
为每个任务添加 `timeoutSeconds` 配置：

```json
{
  "payload": {
    "kind": "agentTurn",
    "timeoutSeconds": 120 // 或 180、240，根据任务复杂度
  }
}
```

**超时配置**:

- `get_top_ai_projects`: 120 秒
- `auto-commit-openclaw`: 180 秒
- `get_xian_weather_forecast`: 240 秒（初始 180 秒不足）
- `backup_cron_results`: 240 秒

**影响文件**:

- `~/.openclaw/cron/jobs.json`
- `src/agents/timeout.ts`
- `src/cli/gateway-rpc.ts`
- `src/cron/types.ts`

---

### 问题 3: Subagent Announce 超时 ✅

**症状**:

- 任务执行成功
- 飞书没有收到任何通知（既无错误也无结果）

**根本原因**:

- `src/agents/subagent-announce.ts` 中硬编码了 60 秒超时
- 即使任务配置了更长的超时时间，announce 流程仍然在 60 秒后超时
- 导致结果无法送达飞书

**修复方案**:
修改 `src/agents/subagent-announce.ts`：

1. `sendAnnounce` 函数接受 `timeoutMs` 参数（默认 60_000）
2. `maybeQueueSubagentAnnounce` 函数接受并传递 `timeoutMs` 参数
3. `runSubagentAnnounceFlow` 使用 `params.timeoutMs` 替代硬编码的 60_000

**代码变更**:

```typescript
// 修改前
const result = await rpc.agentTurn({
  // ...
  timeoutMs: 60_000, // 硬编码
});

// 修改后
const result = await rpc.agentTurn({
  // ...
  timeoutMs: params.timeoutMs ?? 60_000, // 使用配置的超时
});
```

**影响文件**:

- `src/agents/subagent-announce.ts`
- `src/cron/isolated-agent/run.ts`

---

### 问题 4: 主 Agent NO_REPLY 判断 ✅

**症状**:

- 任务执行成功
- Announce 消息到达主 agent
- 主 agent 判断为重复执行，回复 `NO_REPLY`
- 飞书没有收到任务结果

**根本原因**:

- 定时任务结果通过 `runSubagentAnnounceFlow` 发送到主 agent
- 主 agent 的智能判断认为这是重复的通知
- 主 agent 决定不发送消息（`NO_REPLY`）

**修复方案**:
修改 `src/cron/isolated-agent/run.ts`，绕过主 agent，直接发送：

```typescript
// 修改前
if (deliveryPayloadHasStructuredContent) {
  await deliverOutboundPayloads(...);
} else if (synthesizedText) {
  await runSubagentAnnounceFlow(...);  // 通过主 agent
}

// 修改后
if (deliveryPayloadHasStructuredContent || synthesizedText) {
  await deliverOutboundPayloads(...);  // 所有内容直接发送
}
```

**关键改进**:

- 移除了 `runSubagentAnnounceFlow` 调用
- 统一使用 `deliverOutboundPayloads` 直接发送
- 支持纯文本和结构化内容的直接发送
- 保证定时任务结果完整送达

**影响文件**:

- `src/cron/isolated-agent/run.ts`

---

### 问题 5: 天气预报任务超时 ✅

**症状**:

```
Error: gateway timeout after 180000ms
```

**根本原因**:

- 天气预报任务执行时间约 3-4 分钟
- 初始配置的 180 秒超时不足

**修复方案**:
将 `timeoutSeconds` 从 180 增加到 240：

```json
{
  "id": "bad3205a-ddab-4efe-9426-1703b9b757c4",
  "name": "get_xian_weather_forecast",
  "payload": {
    "kind": "agentTurn",
    "timeoutSeconds": 240
  }
}
```

**影响文件**:

- `~/.openclaw/cron/jobs.json`

---

## 最终配置

### 所有 4 个定时任务的统一配置模式

```json
{
  "id": "task-id",
  "name": "task-name",
  "schedule": "cron-expression",
  "delivery": {
    "mode": "announce",
    "channel": "feishu",
    "to": "ou_b3afb7d2133e4d689be523fc48f3d2b3",
    "bestEffort": true
  },
  "payload": {
    "kind": "agentTurn",
    "message": "task-message",
    "timeoutSeconds": 120-240
  }
}
```

### 关键配置说明

1. **delivery.channel**: 明确指定 `"feishu"`
2. **delivery.to**: 飞书用户 ID `"ou_b3afb7d2133e4d689be523fc48f3d2b3"`
3. **delivery.bestEffort**: `true` - 发送失败不影响任务状态
4. **payload.timeoutSeconds**: 根据任务复杂度设置（120-240 秒）

---

## 测试结果

### 测试时间

2026-02-27 11:16 - 11:26

### 测试结果总览

| 任务名称                  | 任务 ID        | 超时配置 | 执行结果 | 输出质量 |
| ------------------------- | -------------- | -------- | -------- | -------- |
| auto-commit-openclaw      | 5e44ba7a...    | 180秒    | ✅ 成功  | ✅ 正常  |
| get_xian_weather_forecast | bad3205a...    | 240秒    | ✅ 成功  | ✅ 优秀  |
| get_top_ai_projects       | 440648af...    | 120秒    | ✅ 成功  | ✅ 优秀  |
| backup_cron_results       | backup-cron... | 240秒    | ✅ 成功  | ✅ 正常  |

### 输出示例

#### 天气预报任务

```markdown
**西安 3天天气预报** (2026年2月27日-3月1日)

| 日期          | 天气    | 最高温 | 最低温 | 降水概率 |
| ------------- | ------- | ------ | ------ | -------- |
| 2月27日(周五) | ⛅️ 多云 | 13.7°C | 7.0°C  | 3%       |
| 2月28日(周六) | 🌧️ 阵雨 | 10.1°C | 7.6°C  | 78%      |
| 3月1日(周日)  | 🌧️ 小雨 | 8.1°C  | 6.0°C  | 94%      |

**提醒**：周末两天有降水，出门记得带伞哦！
```

#### AI 项目排行任务

```markdown
**Top AI Projects on GitHub — February 27, 2026**

1. **openclaw** — 233,476 ⭐
2. **AutoGPT** — 182,050 ⭐
3. **n8n** — 176,576 ⭐
4. **stable-diffusion-webui** — 161,348 ⭐
5. **prompts.chat** — 148,187 ⭐
6. **dify** — 130,494 ⭐
7. **langchain** — 127,572 ⭐
8. **system-prompts-and-models-of-ai-tools** — 125,225 ⭐
9. **open-webui** — 125,074 ⭐
10. **generative-ai-for-beginners** — 107,125 ⭐

OpenClaw still leading the pack! 🚀
```

---

## 代码修改总结

### 1. src/agents/subagent-announce.ts

**修改内容**:

- 添加 `timeoutMs` 参数到 `sendAnnounce` 函数
- 添加 `timeoutMs` 参数到 `maybeQueueSubagentAnnounce` 函数
- 使用 `params.timeoutMs` 替代硬编码的 60_000

**影响**:

- 允许定时任务使用自定义超时时间
- 修复了 announce 流程的超时问题

### 2. src/cron/isolated-agent/run.ts

**修改内容**:

- 移除 `runSubagentAnnounceFlow` 调用
- 统一使用 `deliverOutboundPayloads` 直接发送
- 支持纯文本和结构化内容的直接发送
- 移除未使用的导入

**影响**:

- 定时任务结果直接发送到飞书
- 绕过主 agent 的智能判断
- 保证完整结果送达

### 3. ~/.openclaw/cron/jobs.json

**修改内容**:

- 为所有 4 个任务添加明确的 `delivery.to` 目标
- 为所有 4 个任务添加 `delivery.bestEffort: true`
- 为所有 4 个任务添加 `payload.timeoutSeconds` 配置
- 调整天气预报任务超时从 180 到 240 秒

**影响**:

- 修复错误 798（缺少发送目标）
- 修复错误 520（Gateway 超时）
- 提高系统稳定性

---

## 验证要点

### ✅ 1. 直接发送到飞书

- 所有任务结果直接发送，不经过主 agent
- 保持完整的格式化输出
- 没有被总结或过滤

### ✅ 2. 超时配置生效

- 每个任务使用独立的超时配置
- 超时时间根据任务复杂度调整
- 所有任务都能在配置的时间内完成

### ✅ 3. bestEffort 模式

- 发送失败不影响任务状态
- 错误只记录为 WARN 级别
- 提高系统稳定性

### ✅ 4. 输出质量

- 格式化表格、列表
- 使用 emoji 增强可读性
- 包含实用的提醒信息

---

## 下一步监控

### 1. 等待自动执行验证

明天的定时任务执行时间：

- 00:00 - auto-commit-openclaw
- 08:00 - get_xian_weather_forecast
- 09:00 - get_top_ai_projects
- 10:00 - backup_cron_results

### 2. 验证飞书通知

- 检查是否收到所有 4 个任务的通知
- 确认格式完整、内容准确
- 确认没有错误消息

### 3. 监控稳定性

- 观察一周的执行情况
- 记录任何异常或失败
- 根据需要调整超时配置

---

## 技术要点

### 1. 定时任务执行流程

```
Cron Scheduler
  ↓
Isolated Agent (独立会话)
  ↓
Task Execution (任务执行)
  ↓
Direct Delivery (直接发送)
  ↓
Feishu API (飞书接口)
```

### 2. 超时层级

```
1. Gateway RPC 超时 (默认 60 秒)
   ↓
2. Task 超时 (payload.timeoutSeconds)
   ↓
3. Announce 超时 (已移除，改为直接发送)
```

### 3. 发送模式对比

**修改前**:

```
Task → Announce → Main Agent → Decision → Feishu
                      ↓
                  可能 NO_REPLY
```

**修改后**:

```
Task → Direct Delivery → Feishu
         (绕过主 agent)
```

---

## 相关文档

### 问题分析文档

- `FEISHU-ERROR-798-RESOLVED.md` - 错误 798 分析和解决
- `FEISHU-520-ERROR-ANALYSIS.md` - 错误 520 分析
- `FEISHU-TIMEOUT-SOLUTION.md` - 超时问题解决方案

### 修复过程文档

- `FEISHU-SUBAGENT-ANNOUNCE-TIMEOUT-FIX.md` - Announce 超时修复
- `FEISHU-DIRECT-DELIVERY-TEST.md` - 直接发送测试

### 测试结果文档

- `FEISHU-ANNOUNCE-TEST-RESULT.md` - Announce 测试结果
- `FEISHU-FIX-TEST-REPORT.md` - 修复测试报告
- `FEISHU-ALL-CRON-TASKS-TEST.md` - 所有任务测试
- `FEISHU-FINAL-TEST-SUMMARY.md` - 最终测试总结

### 工具脚本

- `fix-cron-timeout.sh` - 批量修复超时配置
- `fix-weather-timeout.sh` - 修复天气任务超时

---

## 总结

### ✅ 问题完全解决

- 5 个核心问题全部修复
- 4 个定时任务全部测试通过
- 系统恢复正常运行

### ✅ 系统稳定性提升

- bestEffort 模式防止单点故障
- 合理的超时配置
- 清晰的错误日志

### ✅ 用户体验改善

- 收到完整的任务结果
- 格式化输出易读
- 及时的任务通知

### ✅ 代码质量提升

- 移除硬编码超时
- 统一发送逻辑
- 更好的错误处理

---

## 关键经验

1. **超时配置的重要性**: 不同任务需要不同的超时时间，不能一刀切
2. **直接发送 vs 智能判断**: 定时任务结果应该直接发送，不应该被智能过滤
3. **bestEffort 模式**: 提高系统容错性，防止单点故障
4. **完整的测试**: 手动测试所有任务，确保修复生效

---

**修复完成时间**: 2026-02-27 11:26  
**修复状态**: ✅ 完成  
**下次验证**: 2026-02-28 00:00（第一个自动任务执行）

所有飞书定时任务问题已完全解决！🎉
