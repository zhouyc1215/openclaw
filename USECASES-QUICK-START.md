# awesome-openclaw-usecases 快速开始指南

## 📋 概述

已为你准备好 5 个实用的 OpenClaw 用例，可以一键部署到你的 clawdbot。

## 🎯 推荐用例

### 1. 科技新闻摘要 ⭐⭐⭐⭐⭐

- **时间**: 每天 8:00
- **功能**: 自动聚合 AI、开源、科技新闻
- **优势**: 与你现有的"每日 AI 项目"完美互补
- **无需额外配置**

### 2. Reddit 每日摘要 ⭐⭐⭐⭐

- **时间**: 每天 18:00
- **功能**: 热门技术 subreddit 摘要
- **需要**: 安装 `reddit-readonly` skill
- **适合**: 了解社区讨论和趋势

### 3. 自定义晨报 ⭐⭐⭐⭐⭐

- **时间**: 每天 6:30
- **功能**: 天气 + 新闻 + 推荐 + 建议
- **优势**: 整合多个信息源，一站式晨间简报
- **无需额外配置**

### 4. 个人知识库 ⭐⭐⭐⭐

- **触发**: 手动（发送 URL）
- **功能**: 保存和搜索文章、文档
- **需要**: 通过对话配置
- **适合**: 长期知识积累

### 5. AI 财报追踪 ⭐⭐⭐

- **时间**: 每周日 20:00
- **功能**: 追踪科技公司财报日程
- **适合**: 关注 AI 行业动态
- **无需额外配置**

## 🚀 快速部署

### 方法 1：使用自动化脚本（推荐）

```bash
# 运行部署脚本
./deploy-usecases.sh

# 按提示选择要部署的用例
# 1 = 科技新闻摘要
# 2 = Reddit 摘要
# 3 = 自定义晨报
# 4 = 知识库（需手动配置）
# 5 = 财报追踪
# 6 = 全部部署
```

### 方法 2：手动添加

查看 `AWESOME-USECASES-DEPLOYMENT.md` 中的详细配置，手动编辑 `~/.openclaw/cron/jobs.json`。

## 📝 部署后步骤

### 1. 重启 Gateway

```bash
# 方法 1：通过 OpenClaw CLI
openclaw gateway restart

# 方法 2：手动重启（如果方法 1 不可用）
pkill -9 -f openclaw-gateway
nohup openclaw gateway run --bind lan --port 18789 > /tmp/openclaw-gateway.log 2>&1 &
```

### 2. 验证任务已添加

```bash
# 查看所有定时任务
openclaw cron list

# 或直接查看配置文件
cat ~/.openclaw/cron/jobs.json | jq '.jobs[] | {name, schedule, enabled}'
```

### 3. 测试新任务

```bash
# 手动触发任务测试
openclaw cron run <task-name>

# 例如：
openclaw cron run tech_news_digest
openclaw cron run reddit_daily_digest
openclaw cron run custom_morning_brief
```

## 🔧 可选配置

### 安装所需 Skills

某些用例需要额外的 skills：

```bash
# Reddit 摘要需要
openclaw skills install reddit-readonly

# 科技新闻摘要需要（如果 ClawHub 有）
openclaw skills install tech-news-digest

# 知识库需要（如果 ClawHub 有）
openclaw skills install knowledge-base
```

### 配置 API 密钥（可选）

编辑 `~/.openclaw/openclaw.json`，在 `env` 部分添加：

```json
{
  "env": {
    "GEMINI_API_KEY": "...",
    "MINIMAX_API_KEY": "...",

    // 以下为可选，提升功能
    "X_BEARER_TOKEN": "your_twitter_token",
    "BRAVE_API_KEY": "your_brave_api_key",
    "GITHUB_TOKEN": "your_github_token"
  }
}
```

## 📊 任务时间表

部署所有用例后，你的每日时间表：

```
06:30 - 自定义晨报（天气 + 新闻 + 推荐）
07:00 - 西安天气预报（原有任务）
08:00 - 科技新闻摘要（新）
09:00 - 每日 AI 项目（原有任务）
18:00 - Reddit 每日摘要（新）
00:00 - 自动提交代码（原有任务）
00:00 - 备份定时任务结果（原有任务）

每周日 20:00 - AI 财报追踪（新）
```

## ⚠️ 注意事项

1. **超时设置**: 所有新任务已配置合理的超时时间（180-240 秒）
2. **bestEffort**: 所有任务启用了 `bestEffort: true`，失败不会发送错误消息
3. **时区**: 使用 `Asia/Shanghai` 时区
4. **并发**: 不会与现有任务冲突，时间已错开
5. **存储**: 知识库功能会占用存储空间，定期清理

## 🎨 自定义

### 修改任务时间

编辑 `~/.openclaw/cron/jobs.json`，修改 `schedule.expr`：

```json
"schedule": {
  "kind": "cron",
  "expr": "0 8 * * *",  // 分 时 日 月 周
  "tz": "Asia/Shanghai"
}
```

### 修改任务内容

编辑 `payload.message` 字段，调整 prompt 内容。

### 禁用任务

```json
"enabled": false
```

或使用命令：

```bash
openclaw cron disable <task-name>
```

## 🐛 故障排查

### 任务未执行

```bash
# 查看 Gateway 日志
tail -f /tmp/openclaw-gateway.log

# 查看任务状态
openclaw cron list

# 检查任务详情
cat ~/.openclaw/cron/jobs.json | jq '.jobs[] | select(.name=="tech_news_digest")'
```

### 任务超时

增加 `timeoutSeconds` 值：

```json
"payload": {
  "timeoutSeconds": 300  // 增加到 5 分钟
}
```

### 消息未送达飞书

检查：

1. Gateway 是否运行
2. 飞书配置是否正确
3. `delivery.to` 是否为正确的用户 ID

## 📚 更多资源

- [awesome-openclaw-usecases GitHub](https://github.com/hesamsheikh/awesome-openclaw-usecases)
- [OpenClaw 文档](https://docs.openclaw.ai)
- [ClawHub Skills](https://clawhub.ai)

## 💬 反馈

部署后，通过飞书告诉 clawdbot 你的反馈：

```
我喜欢/不喜欢 [任务名称]，因为...
请调整 [任务名称] 的 [具体内容]
```

Clawdbot 会学习你的偏好并优化内容！
